<?xml version="1.0" encoding="UTF-8"?>

<BaseClasses:View
    x:Class="RCS.PortableShop.Views.MainView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:BaseClasses="clr-namespace:RCS.PortableShop.Common.Views;assembly=RCS.PortableShop.Common"
    xmlns:Views="clr-namespace:RCS.PortableShop.Views;assembly=RCS.PortableShop"
    xmlns:Controls="clr-namespace:RCS.PortableShop.Common.Controls;assembly=RCS.PortableShop.Common"
    xmlns:Localization="clr-namespace:RCS.PortableShop.Localization;assembly=RCS.PortableShop"
    >

    <Grid VerticalOptions="Fill">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <BaseClasses:View Grid.Row="0" Content="{Binding MainRegionContent}"/>

        <!--HACK Setting BackgroundColor here because of https://bugzilla.xamarin.com/show_bug.cgi?id=25943 -->
        <!--Note that only a limited number of Color names are currently supported.-->
        <Controls:ContentButton Grid.Row="1" Command="{Binding ShowCartCommand}" BackgroundColor="#F4FA58" Padding="5">
            <Controls:ContentButton.Content>
                <Views:ShoppingCartSummaryView/>
            </Controls:ContentButton.Content>
        </Controls:ContentButton>
    </Grid>
</BaseClasses:View>