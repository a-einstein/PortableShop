<?xml version="1.0" encoding="UTF-8"?>

<BaseClasses:View
    x:Class="RCS.PortableShop.Views.MainView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:BaseClasses="clr-namespace:RCS.PortableShop.Common.Views;assembly=RCS.PortableShop.Common"
    xmlns:Localization="clr-namespace:RCS.PortableShop.Localization;assembly=RCS.PortableShop"
    >

    <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
        <!--Works.-->
        <Label Text="Welcome to PortableShop!" />
        
        <!--Works.-->
        <Label Text="{Binding ViewModelText}" />
        
        <!--TODO >> Crashes. Get this working. The sample works. The problem seems to be the layered structure or the base class here.-->
        <Label Text="{Localization:Translate NavigateAbout}" />
    </StackLayout>
</BaseClasses:View>